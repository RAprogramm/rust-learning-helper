fn main() {                          // начало программы
    let nums = vec![5, 10, 15];      // создаём вектор чисел

    for n in nums {                  // перебираем каждый элемент
        println!("Элемент: {}", n);  // выводим элемент
    }
}
