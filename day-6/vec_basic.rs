fn main() {                          // начало программы
    let numbers = vec![1, 2, 3];     // создаём вектор из трёх чисел

    println!("{:?}", numbers);       // выводим весь вектор (отладочный формат)
}
